# --- Cấu hình Discord Bot ---
DISCORD_TOKEN=YOUR_DISCORD_BOT_TOKEN_HERE
# Token của bot Discord của bạn. Lấy từ Discord Developer Portal (Applications -> Your Bot -> Bot -> Reset Token).

CLIENT_ID=YOUR_BOT_CLIENT_ID_HERE
# Client ID của bot Discord của bạn. Lấy từ Discord Developer Portal (Applications -> Your Bot -> General Information).

GUILD_ID=YOUR_DEVELOPMENT_GUILD_ID_HERE
# (TÙY CHỌN) ID của Guild (server) mà bạn dùng để phát triển/thử nghiệm bot.
# Các lệnh Slash Commands sẽ được đăng ký tức thì vào Guild này.
# Nếu bạn muốn đăng ký lệnh toàn cục (cho tất cả server), có thể bỏ trống hoặc xóa dòng này,
# nhưng việc đăng ký global commands sẽ mất đến 1 giờ để có hiệu lực.

ADMIN_LOG_CHANNEL_ID=YOUR_ADMIN_LOG_CHANNEL_ID_HERE
# ID của kênh văn bản trên Discord nơi bot sẽ gửi thông báo lỗi nghiêm trọng hoặc các log quan trọng cho admin.
# Để lấy Channel ID: Bật Developer Mode trong Discord (User Settings -> Advanced -> Developer Mode),
# sau đó chuột phải vào kênh và chọn "Copy ID".

# --- Cấu hình Python Backend API ---
# URL của API backend Python của bạn.
# Nếu bạn triển khai Python Backend trên Railway dưới dạng một service riêng,
# sử dụng URL nội bộ của Railway để giao tiếp giữa các service, ví dụ:
# PYTHON_API_URL=http://your-python-backend-service-name.railway.internal:5000/api/get_music_info
# (Thay 'your-python-backend-service-name' bằng tên service backend Python của bạn trên Railway)
# Hoặc nếu bạn đã mở cổng công khai cho backend Python:
# PYTHON_API_URL=https://your-python-backend-domain.railway.app/api/get_music_info

# Lưu ý: Cần thêm cả endpoint vào đây để bot biết gọi vào đâu cho music info.
# Ví dụ: nếu endpoint là /api/get_music_info, thì để:
# PYTHON_API_URL=http://your-python-backend-service-name.railway.internal:5000/api/get_music_info
#
# Đối với Autocomplete, bot sẽ thay thế "/api/get_music_info" bằng "/api/suggest"
# nên bạn chỉ cần cung cấp URL gốc cho API chính.
PYTHON_API_BASE_URL=http://your-python-backend-service-name.railway.internal:5000 
# Ví dụ trên Railway, thay 'your-python-backend-service-name' bằng tên service backend Python của bạn.
# Hoặc nếu bạn muốn dùng public domain (ít được khuyến khích cho giao tiếp nội bộ):
# PYTHON_API_BASE_URL=https://your-python-backend-domain.railway.app

# --- Tùy chọn khác (nếu có) ---
# Tên Bot (tùy chọn, cho mục đích log hoặc display)
BOT_NAME=HybridMusicBot

# Cấp độ log cho Node.js bot (debug, info, warn, error)
LOG_LEVEL=info

# Các biến môi trường khác bạn có thể cần thêm vào sau này (ví dụ: database credentials, Spotify API keys nếu xử lý trực tiếp trong Node.js)
# DATABASE_URL=mongodb+srv://hadiepml:<db_password>@cluster0.mixmt.mongodb.net/?retryWrites=true&w=majority&appName=Cluster0
# SPOTIFY_CLIENT_ID=07c6723a01bc4022814644e62e8b1d60
# SPOTIFY_CLIENT_SECRET=4092db76241e4268bbba441b926ecfaf
