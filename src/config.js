// src/config.js
export const config = {
    prefix: '!', // Tiền tố cho các lệnh truyền thống (nếu có)
    embedColor: '#0099ff', // Màu mặc định cho các embed
    ownerId: '1160073807919317012', // ID của bạn để tạo lệnh owner-only
    // Thêm các cấu hình khác nếu cần
};
